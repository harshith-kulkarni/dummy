smartassignment docker
# Use official Tomcat base image
FROM tomcat:9.0

# Remove default webapps to keep it clean (optional but recommended)
RUN rm -rf /usr/local/tomcat/webapps/*

# Copy your WAR file into Tomcat's webapps directory
COPY target/Smart-Assessment-Hub.war /usr/local/tomcat/webapps/Smart-Assessment-Hub.war

# Expose port 8080 for web access
EXPOSE 8080

# Start Tomcat server
CMD ["catalina.sh", "run"]



docker exec -it sah-mysql mysql -u root -p
CREATE TABLE posts (id INT PRIMARY KEY, content TEXT);
add table in my sql




dockercompose.yml
version: '3.8'

services:
  tomcat:
    image: harshith/smarthub-image
    container_name: sah-tomcat
    ports:
      - "8080:8080"
    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    container_name: sah-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: sahdb
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: